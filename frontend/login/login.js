const form=document.querySelector("#authForm"),message=document.querySelector("#message"),displayLabel=document.querySelector("#displayNameLabel");let setup=false;
async function initialize(){try{const response=await fetch("/auth/status");const data=await response.json();setup=data.needs_setup;if(data.user){location.href=data.user.role==="admin"?"/app/admin/":"/app/";return}if(setup){document.querySelector("#formEyebrow").textContent="FIRST-TIME SETUP";document.querySelector("#formTitle").textContent="建立管理者";document.querySelector("#formIntro").textContent="首次啟動，請建立本機管理帳號。";document.querySelector("#submitButton").textContent="建立並登入";displayLabel.classList.remove("hidden");}}catch{show("無法連線至後端服務。")}}
function show(text){message.textContent=text;message.classList.remove("hidden")}
form.addEventListener("submit",async(event)=>{event.preventDefault();message.classList.add("hidden");const body={username:document.querySelector("#username").value,password:document.querySelector("#password").value};if(setup){body.display_name=document.querySelector("#displayName").value;body.role="admin"}try{const response=await fetch(setup?"/auth/setup":"/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.detail||"登入失敗");const next=new URLSearchParams(location.search).get("next");location.href=next&&next.startsWith("/app/")?next:(data.user.role==="admin"?"/app/admin/":"/app/");}catch(error){show(error.message)}});initialize();
