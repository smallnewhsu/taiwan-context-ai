# 聲入其境：直接錄音與人工修正記憶整合版

將 ZIP 解壓縮到 `C:\taiwan-context-ai` 並覆蓋同名檔案，重新啟動 FastAPI。

瀏覽器請使用：`http://127.0.0.1:8000/app/speech/`

功能：

1. 點擊中央麥克風直接錄音。
2. 可停止、播放、重新錄製，或改選既有音檔。
3. 錄音採 WebM/Opus，因此後端已加入 WebM 與 OGG 支援。
4. 人工核准後，相同音檔會套用已審核修正內容。

舊審核紀錄不含音檔 SHA-256，必須於更新後重新確認及核准一次。
